suggestedBy:
- 'account.unhosted.org'
- 'surf.unhosted.org'
- 'libredocs.org'

users.unhosted.org - POST a user address, get a storageInfo (or null) back. extra field in there, 'suggestedBy'.
account.unhosted.org - log in with BrowserID, choose:
- freestyle
- blueprints:
  - ownCloud instance (path to directory; will test  existance of remoteStorage app)
  - drinkscabinet instance (e.g. if running on a different port or an ugly domain name)
future:
  - dropbox
  - IrisCouch pimped subdomain
  - CouchDB instance, will pimp
log in with webmaster@domain to set a per-domain default.
